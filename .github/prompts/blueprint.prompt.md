---
agent: agent
description: "Update or refine the blueprint only, using the blueprint-workflow skill in blueprint mode."
---

Run the `blueprint` skill in **blueprint mode**: edit only blueprint files (paths matched by `.blueprintfiles`) and leave implementation files unchanged.

User request:

${input}
