---
agent: agent
description: "Implement changes described by the blueprint, using the blueprint-workflow skill in implement mode."
---

Run the `blueprint` skill in **implement mode**: treat blueprint files (paths matched by `.blueprintfiles`) as the specification, and make changes only in implementation files.

Implementation request:

${input}
